As a customer, I want to receive an email confirmation after I've made a reservation.
=We only take reservations for the top of the hour, from 4pm-11pm.
<% if session[:user_id] %>
<h1>Listing reservations</h1>

<table>
  <tr>
    <th>Your reservation date</th>
    <th>Your party number</th>
    <th>User</th>
    <th>Change your reservation</th>
  </tr>

<% @reservations.each do |reservation| %>
  <tr>
    <td><%= reservation.appoint_date %></td>
    <td><%= reservation.party %></td>
    <td><%= reservation.user_id %></td>
    <td><%= link_to 'Edit', edit_reservation_path(reservation) %></td>
    <td><%= link_to 'Destroy', reservation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reservation', new_reservation_path %>

<% else %>
    <li><%= link_to "Please Sign Up to make a reservation!", new_user_url %></li>
    <li><%= link_to "If you have an account, please sign in", sign_in_url %></li>
<% end %>
